[project]
name = "permis-course"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "fsrs",
    "jinja2",
    "markdown-it-py",
]

[dependency-groups]
dev = [
    "pytest",
    "ruff",
    "pyright",
]

[tool.pytest.ini_options]
pythonpath = ["."]
